---
name: Deploy AWS Compute EC2 Single Region

description: |
    This GitHub Action automates the deployment of the aws/compute/ec2-single-region reference architecture using Terraform.

inputs:
    cluster-name:
        description: Name of the EC2 cluster to deploy
        required: true
    aws-region:
        description: AWS region where the EC2 cluster will be deployed
        required: true
    tags:
        description: Tags to apply to the cluster and related resources, in JSON format
        default: '{}'
    s3-backend-bucket:
        description: Name of the S3 bucket to store Terraform state
        required: true
    s3-bucket-region:
        description: Region of the bucket containing the resources states, if not set, will fallback on aws-region
    s3-bucket-key-prefix:
        description: Key prefix of the bucket containing the resources states. It must contain a / at the end e.g 'my-prefix/'.
        default: ''
    tf-modules-revision:
        description: Git revision of the tf modules to use
        default: main
        required: true
    tf-modules-path:
        description: Path where the tf ec2 arch will be cloned
        default: ./.action-tf-modules/aws-compute-ec2-single-region-create/
        required: true
    tf-modules-name:
        description: Name of the tf modules to use, the folder to refer to - cluster / vpn
        default: cluster
        required: true
    ref-arch:
        description: Reference architecture to deploy
        default: ec2-single-region
        required: false
outputs:
    terraform-state-url:
        description: URL of the Terraform state file in the S3 bucket
        value: ${{ steps.set-terraform-variables.outputs.terraform-state-url }}

runs:
    using: composite
    steps:
        - name: Checkout Repository
          uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
          with:
              repository: camunda/camunda-deployment-references
              ref: ${{ inputs.tf-modules-revision }}
              path: ${{ inputs.tf-modules-path }}
              fetch-depth: 0

        - name: Install asdf tools with cache for the project
          uses: camunda/infraex-common-config/.github/actions/asdf-install-tooling@9d485973624ec326ef0e92c73191b6c0f26ad39e # 2.3.6

        - name: Set Terraform variables
          id: set-terraform-variables
          shell: bash
          run: |  # zizmor: ignore[github-env]
              set -euo pipefail
              export TFSTATE_BUCKET="${INPUTS_S3_BACKEND_BUCKET}"
              export TFSTATE_BASE_KEY="${INPUTS_S3_BUCKET_KEY_PREFIX}tfstate-${INPUTS_CLUSTER_NAME}/"

              if [ -z "${INPUTS_S3_BUCKET_REGION}" ]; then
                export TFSTATE_REGION="$AWS_REGION"
              else
                export TFSTATE_REGION="${INPUTS_S3_BUCKET_REGION}"
              fi

              echo "TFSTATE_BUCKET=${TFSTATE_BUCKET}" >> "$GITHUB_OUTPUT"
              echo "TFSTATE_REGION=${TFSTATE_REGION}" >> "$GITHUB_OUTPUT"
              echo "TFSTATE_BASE_KEY=${TFSTATE_BASE_KEY}" >> "$GITHUB_OUTPUT"

              terraform_state_url="s3://${TFSTATE_BUCKET}/${TFSTATE_BASE_KEY}${INPUTS_TF_MODULES_NAME}.tfstate"
              echo "terraform-state-url=${terraform_state_url}" >> "$GITHUB_OUTPUT"

              echo "TF_MODULE_DIR=${INPUTS_TF_MODULES_PATH}/aws/compute/${INPUTS_REF_ARCH}/terraform/${INPUTS_TF_MODULES_NAME}" >> "$GITHUB_ENV"
          env:
              INPUTS_S3_BACKEND_BUCKET: ${{ inputs.s3-backend-bucket }}
              INPUTS_S3_BUCKET_KEY_PREFIX: ${{ inputs.s3-bucket-key-prefix }}
              INPUTS_CLUSTER_NAME: ${{ inputs.cluster-name }}
              INPUTS_S3_BUCKET_REGION: ${{ inputs.s3-bucket-region }}
              INPUTS_TF_MODULES_NAME: ${{ inputs.tf-modules-name }}
              INPUTS_TF_MODULES_PATH: ${{ inputs.tf-modules-path }}
              INPUTS_REF_ARCH: ${{ inputs.ref-arch }}

        - name: Copy CI fixtures
          shell: bash
          working-directory: ${{ env.TF_MODULE_DIR }}
          run: |
              set -euo pipefail
              cp -r ./test/fixtures/ci/fixture_* .

        - name: Terraform Init
          id: init
          working-directory: ${{ env.TF_MODULE_DIR }}
          shell: bash
          run: |
              set -euo pipefail

              terraform version

              terraform init \
                -backend-config="bucket=${TFSTATE_BUCKET}" \
                -backend-config="key=${TFSTATE_BASE_KEY}${INPUTS_TF_MODULES_NAME}.tfstate" \
                -backend-config="region=${TFSTATE_REGION}"

              terraform validate -no-color
          env:
              TFSTATE_BASE_KEY: ${{ steps.set-terraform-variables.outputs.TFSTATE_BASE_KEY }}
              TFSTATE_BUCKET: ${{ steps.set-terraform-variables.outputs.TFSTATE_BUCKET }}
              TFSTATE_REGION: ${{ steps.set-terraform-variables.outputs.TFSTATE_REGION }}
              INPUTS_TF_MODULES_NAME: ${{ inputs.tf-modules-name }}

        - name: Sanitize Tags
          id: sanitize-tags
          uses: ./.github/actions/internal-sanitize-tags
          with:
              raw-tags: ${{ inputs.tags }}

        - name: Generate tfvars file
          id: generate-tfvars
          shell: bash
          working-directory: ${{ env.TF_MODULE_DIR }}
          run: |
              set -euo pipefail

              # cut name as some resources have a character limit that we may run into
              # e.g. target groups have a max length of 32 characters
              # with 15 characters we're adding in Terraform
              # therefore cut to 17 characters to be on the safe side
              PREFIX=$(echo "${INPUTS_CLUSTER_NAME}" | cut -c1-17)

              cat > terraform.tfvars <<EOF
              default_tags = ${SANITIZED_TAGS}
              prefix       = "${PREFIX}"
              EOF
          env:
              SANITIZED_TAGS: ${{ steps.sanitize-tags.outputs.sanitized_tags }}
              INPUTS_CLUSTER_NAME: ${{ inputs.cluster-name }}

        - name: Terraform Plan
          id: plan
          working-directory: ${{ env.TF_MODULE_DIR }}
          shell: bash
          run: |
              set -euo pipefail
              export AWS_REGION="${INPUTS_AWS_REGION}"

              terraform plan -no-color \
                -var-file=terraform.tfvars \
                -out tf.plan
          env:
              INPUTS_AWS_REGION: ${{ inputs.aws-region }}

        - name: Terraform Apply
          id: apply
          working-directory: ${{ env.TF_MODULE_DIR }}
          shell: bash
          run: |
              set -euo pipefail
              terraform apply -no-color tf.plan

        - name: Terraform Drift Detection
          uses: ./.github/actions/internal-terraform-drift-detect
          id: drift-detect
          with:
              working-directory: ${{ env.TF_MODULE_DIR }}
              plan-extra-args: |
                  -var-file=terraform.tfvars
