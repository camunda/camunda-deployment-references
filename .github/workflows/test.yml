---
name: Test Bash Functions with Custom Shell

on:
    push:
    workflow_dispatch:

jobs:
    test-bash-funcs:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Test Bash functions
              shell: .github/scripts/custom-bash.sh
              run: |
                  say_hello
                  show_time
