---
name: Internal - Global - Pull Request TODO checker
on:
    pull_request_review_comment:
        types: [edited, deleted]
    pull_request:
        types: [opened, synchronize, reopened]

jobs:

    # TODO: pin on main
    call-todo-checker:
        uses: camunda/infraex-common-config/.github/workflows/todo-checker-global.yml@feature/todo-checker
        secrets: inherit
