---
# OpenShift Domain Configuration
#
# This file contains OpenShift-specific ingress configuration.

global:
    identity:
        auth:
            identity:
                redirectUrl: https://${DOMAIN_NAME}/managementidentity
            console:
                redirectUrl: https://${DOMAIN_NAME}/console
            webModeler:
                redirectUrl: https://${DOMAIN_NAME}/modeler
            optimize:
                redirectUrl: https://${DOMAIN_NAME}/optimize

    # the ingress is managed directly by the Route manifests
    ingress:
        enabled: true
        className: openshift-default
        host: ${DOMAIN_NAME}
        tls:
            enabled: true
            # explicitly '-' as secret name as we rely on the IngressOperator to handle the TLS secret
            # if you use a custom certificate, you might want to fill this value
            secretName: '-'
        annotations:
            route.openshift.io/termination: edge
            # enforce HSTS
            haproxy.router.openshift.io/hsts_header: max-age=31536000; includeSubDomains;preload

identity:
    contextPath: /managementidentity
    fullURL: https://${DOMAIN_NAME}/managementidentity

webModeler:
    contextPath: /modeler

optimize:
    contextPath: /optimize

orchestration:
    contextPath: /

console:
    contextPath: /console

connectors:
    contextPath: /connectors
